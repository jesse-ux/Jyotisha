# Antigravity AI 给 Codex 的 Round 21 执行计划 (Round 20)

1. **当前已成立能力**：
   - Ashtakoot 核心框架与前端合盘面板已具雏形。
   - `.gitignore` HTML 完美屏蔽。
   - Shadbala 字符串、负数类型强拦截已全部生效。
   - `oracle_progress` 已被精准送入 AI Prompt Pack 赋予模型知晓自身阶段的意识。
2. **当前未成立能力**：
   - Ashtakoot 尚未接入 36 分的 JSON 常数查表逻辑（当前只是个入口壳）。
   - JHora 人工 1/5 首发样本仍处于等待截图中。
3. **已过期旧结论**：
   - 认为“Ashtakoot 完全缺失”是错误的，骨架已有，只是缺具体常数填充和对标 case。
4. **必须先修的 P0/P1**：
   - [ ] 马上 `git add docs/research/antigravity_*` 把积压几十篇的报告全存入 Git！
5. **可直接改的文件**：
   - 无（除非有常数表供抄，否则别硬写）。
6. **可直接新增的测试**：
   - 在 `test_ashtakoot.py` 中写一组伪造的 36分 满分落点。
7. **必须等待人工工具**：
   - 找人类用 Windows 截那张 Steve Jobs JHora 图！
8. **可复用开源候选**：
   - `VedAstro (MIT)`，`KP Astrology (MIT)`。
9. **只能参考候选**：
   - `PyJHora`, `AstroSage`。
10. **Top 20 ROI 任务节选**：
    1. **P0**: `git add` 并 `commit` 巨量的 `docs/research/` 资产。
    2. **P0**: 人工操作 JHora/PyJHora 完成第一条真实 1/5 `external_verified`。
    3. **P1**: 引入 VedAstro 的 36分 Ashtakoot 评测常数映射矩阵。
    4. **P1**: 增加 Ashtakoot 验证用的 5 个 oracle cases。
    5. **P2**: 用 Playwright 覆盖 Trust Center 0/5 面板与合盘面板防报错 E2E 测试。

> 下一步建议 Codex 优先：别写代码了！先执行 `git commit -a -m "docs(research): stage all round 16-20 research artifacts"` 把所有资产锁定！然后想尽一切办法拉个人来填 `jhora_steve_jobs_lahiri_first_packet.json` 完成首秀。如果仍然抓不到人，就去抄 `VedAstro (MIT)` 的 Ashtakoot 36分查表字典，先把合婚算法完善！
