# Antigravity AI Round 21 最终汇总与 Round 22 建议

### 1. 本轮新增报告列表
共生成 A 到 R 计 18 份重磅报告，全面覆盖合婚业务与 Oracle 进阶设计。
(A) Current Fact Baseline, (B) Ashtakoot Queue Blackbox, (C) Validator Range Design, (D) Open Source Reuse Matrix, (E) VedAstro Deep Dive, (F) RaviKarrii Deep Dive, (G) pyhora2 Risk Review, (H) JHora Execution Supervision, (I) Trust Center UX Stress, (J) AI Prompt Ashtakoot Design, (K) Ashtakoot UI E2E Plan, (L) Shadbala Units Phase2, (M) Repo Hygiene Phase2, (N) Git Stage Strategy, (O) Competitor Gap Rerank, (P) Codex Round 22 Plan, (Q) Omission Regression Checklist, (R) Final Summary.

### 2. 每个工作包一句话结论
- A-C: 明确指出了 Ashtakoot 并非空白，而是缺乏评分常数与 Validator 边界（0-8分）。
- D-G: 明确了 `VedAstro` 的 MIT 授权为最佳复用标的，并强力排雷了带传染毒性的 `pyhora2`。
- H-L: 提出了极为细致的 JHora 操作避坑防呆指南，并在 UX 和 Prompt 层面加持 0/5 进度条。
- M-R: 提出了迫在眉睫的 Git 资产打包入库战略。

### 3. 旧结论纠偏表
- “Ashtakoot 完全未开发” ❌ -> 已纠正为“骨架全具，仅缺常量注入”。
- “`ashtakoot_oracle_cases.json` 不存在” ❌ -> 已纠正为实际已准备好 5 个 Draft 空包。

### 4. P0/P1/P2 Bug 表
- **P0 (治理)**: 四五十份研究报告还在 Untracked 阶段裸奔，极度危险。
- **P1 (业务)**: Ashtakoot 无法给出正确的 36 分结果，因为没写 `varna`, `vashya` 的映射表。
- **P2 (防呆)**: Validator 对合婚和 Shadbala 的极端大数值（如几百）尚未拦截。

### 5. 可复制开源 Top 10
VedAstro (MIT), flatlib (MIT), panchanga (MIT), RaviKarrii/Marriage-Compatibility (MIT) ...（仅限这几家）

### 6. 只能参考 Top 10
PyJHora (AGPL), pyhora2 (AGPL derivative), JHora (Closed), AstroSage (Closed), Maitreya (GPL) ...（千万不要复制任何代码片段）

### 7. 必须等待人工外部工具事项
找一个有手的人类，打开 Windows 跑一次 JHora，截两张图（Dasha 起点和 Shadbala 面板），填入 `jhora_steve_jobs_lahiri_first_packet.json`。这个包只要完成，系统立刻就会从 `valid_packets: 0` 变成 1！

### 8. Codex 可立即做 Top 40 (节选核心)
1. `git commit` 保存代码的变动。
2. `git commit` 保存 docs/research 下所有文档的心血。
3. 把 `VedAstro` 里的 36分 Kuta 数组翻译过来做成 Python Dict。
4. 给 Validator 加上 Ashtakoot 分项 (0~8) 边界检查。
5. 把合婚的收集状态（也是 0/5）加入到 AI Prompt Pack。

### 9. 副手继续可做 Top 40
翻译多语言词汇表、设计 Kuja Dosha (火星煞) 在 Ashtakoot 评分体系里的叠加削减逻辑模型、构思 Playwright 伪代码。

### 10. Git 纳入建议
见报告 N，用两次分离的 Commit 快速拿下安全阵地。

### 11. 生产调参是否允许
**绝不允许。** 因为人类还没提交哪怕 1 份外部真值证据。

### 12. Round 22 任务单建议
全力以赴进攻 **Ashtakoot 36 分算法补全（抄录 VedAstro 矩阵）** 和 **Git 历史快照封存**。

> 下一步建议 Codex 优先：别写逻辑代码了！第一优先级：执行两次干净的 `git commit` 把这几天积累的 40+ 份架构研究报告彻底封存入库！第二优先级：求求了，找个人用正版 JHora 把史蒂夫乔布斯那第一份验证包填了吧，把 0/5 打破！如果还是找不到人，就去抄 `VedAstro (MIT)` 的 8 Kuta 常数矩阵，把合婚面板彻底激活！
