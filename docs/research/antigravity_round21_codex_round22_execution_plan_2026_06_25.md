# Antigravity AI 给 Codex 的 Round 22 执行计划 (Round 21)

1. **Top 10 P0/P1**:
   - P0: 执行双段 `git commit` 保存工作树。
   - P0: 等待人类操作 Windows 给出一套 JHora 真截图并填平 JSON。
   - P1: 从 VedAstro 移植 8 Kuta 36 分常量查表矩阵。
   - P1: 给 Validator 增加 Ashtakoot 单项边界验证 (0~8)。
   - P1: 给 Validator 增加 Shadbala 总分校验 (sum ≈ total)。
   - P2: 在 AI Prompt Pack 中挂载 `ashtakoot_oracle_progress`。
2. **影响面**: 解决合婚计分，能瞬间抹平与 AstroSage 等大站的痛点差距。
3. **具体文件**: `ashtakoot.py`, `oracle_evidence_validator.py`, `jyotish_engine.py`。
4. **具体测试**: `test_ashtakoot.py`。
5. **需要联网**: 查阅 VedAstro 的 GitHub 取 C# 字典代码。
6. **人工外部工具**: 必须等真人跑 JHora 第一单！
7. **用户确认**: 提交 40 多份文件入 Git 前，最好给用户发个知情提示。
8. **最小实现**: 别写花里胡哨的代码，直接把 `VedAstro` 矩阵抄过来做成 Python 字典。
9. **不做事项**: 绝不复制 AGPL 的 `PyJHora` 代码。
10. **验收命令**: `pytest tests/test_ashtakoot.py` 和 `pytest tests/test_oracle_evidence_validator.py`。
11. **建议 commit 拆分**: 见 Report N（第一发代码，第二发文档）。
12. **副手下一轮**: 处理多语言词典等体力活。
