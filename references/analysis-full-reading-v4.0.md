# 匿名化说明：个人完整解盘报告已移除

该文件曾包含真实用户完整星盘分析或技法审查内容。为避免在 skill 或公开仓库中暴露个人出生信息、人生事件、关系状态、职业经历、项目背景或个人化推断，原内容已移除。

后续验证请使用：

- 公开 AA 级名人案例；
- `tests/golden/golden_cases.json` 的虚构 smoke case；
- 当前会话中用户主动提供的数据（只在当前会话内使用，不写入 skill 文件）。

**隐私规则**：不得将真实用户个人资料、个案结论或回测事件写入 references、assets、tests、CHANGELOG 或任何公开仓库文件。
